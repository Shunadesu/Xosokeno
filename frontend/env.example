# API Configuration
# For local development (with proxy)
VITE_API_URL=/api
# For production - Vercel sẽ proxy /api đến backend Render
# Không cần set VITE_API_URL, mặc định sẽ dùng /api
# VITE_SOCKET_URL=/socket.io

# Environment
VITE_NODE_ENV=development

# Feature Flags (optional)
# VITE_ENABLE_DEBUG=true
# VITE_ENABLE_ANALYTICS=false


